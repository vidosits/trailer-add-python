name: "Add Python automatically"
description: |
  "This Trailer.dev recommendation will automatically add Python as a dependency,
   if a PyPI dependency is added without Python being added first. This recommendation
   will automatically set Python to the latest version from conda-forge"
runtime: go
import: github.com/vidosits/trailer-add-python
type: image
